{{extend 'layout.html'}}

<h1>Full details of all Suppliers

<div class="copyright pull-right"><img src="/Online_Shop_Management/static/images/supply.png" height="100" width="100" alt="Burden Help" align="right"></div>

</h1>
<table class='table'>
       <tr>
       <th>Name</th>
       <th>Contact no.</th>
       <th>E-mail</th>
       <th>Address</th>
       <th>Transaction(Till now)</th>
       </tr>
        {{for row in rows:}}
              {{if row.Name!="User":}}  
               {{if row.s_city==request.vars.city or request.vars.city=='All': }}        
                 {{if row.country==request.vars.country or request.vars.country=='All': }}
                     {{if row.s_state==request.vars.city or request.vars.state=='All': }}
                        {{if row.s_transaction>request.vars.transaction_above or request.vars.transaction_above=="None": }}
                                {{if row.s_transaction<=request.vars.transaction_below or request.vars.transaction_below=="None": }}   
             <tr>
             <td>{{=row.Name}}</td>
             <td>{{=row.s_no}}</td>
             <td>{{=row.s_email}}</td>
             <td>{{=row.s_address}},{{=row.s_city}},{{=row.s_state}},{{=row.country}},{{=row.s_zip}}</td>
             <td>{{=row.s_transaction}}</td>
             </tr>  
           {{pass}}
           {{pass}}
           {{pass}}
           {{pass}}
           {{pass}}
           {{pass}}    
        {{pass}}        
</table>        


{{link = URL(c ='default', f = 'supplier')}}
<h3><a href="{{=link}}"> Done</a></h3>


        <h5><center><p><-----------------------------------------------------------------Done---------------------------------------------------------------------></p></center></h5>       

